# 课堂手机管理系统 - 实验报告

## 一、需求分析

### 1.1 项目背景
随着智能手机的普及，学生在课堂上使用手机的现象越来越普遍，严重影响了教学效果。为了规范课堂秩序，提高教学质量，需要开发一套课堂手机管理系统，实现对课堂手机使用的有效管理。

### 1.2 系统目标
- 实现教师对学生课堂手机使用的登记和管理
- 实现手机没收记录的登记、修改、删除功能
- 实现手机移交状态的跟踪管理
- 提供多维度的统计报表功能
- 支持基础数据的导入和管理

### 1.3 功能需求
1. **教师角色功能**
   - 查看近两日授课课堂
   - 登记没收手机记录
   - 查看、修改、删除没收记录
   - 查看统计报表

2. **统计报表功能**
   - 按班级统计没收数量
   - 按教师统计没收数量
   - 按课程统计没收数量
   - 按周次统计没收数量
   - 按节次统计没收数量
   - 按学期统计没收数量
   - 按时间范围统计
   - 按移交状态统计

3. **基础数据管理**
   - 学期信息管理
   - 班级信息管理
   - 教室信息管理
   - 教师信息管理
   - 课表信息管理

## 二、数据库设计

### 2.1 数据库表结构

#### 学期表 (sys_semester)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| semester_name | VARCHAR(50) | 学期名称 |
| start_date | DATE | 学期开始日期 |
| end_date | DATE | 学期结束日期 |
| status | TINYINT | 状态：0-禁用，1-启用 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

#### 班级表 (sys_teaching_class)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| class_name | VARCHAR(100) | 班级名称 |
| grade | VARCHAR(20) | 年级 |
| major | VARCHAR(50) | 专业 |
| student_count | INT | 学生人数 |
| semester_id | BIGINT | 所属学期ID |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

#### 教室表 (sys_classroom)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| classroom_name | VARCHAR(50) | 教室名称 |
| building | VARCHAR(50) | 教学楼 |
| floor | INT | 楼层 |
| capacity | INT | 容量 |
| cabinet_location | VARCHAR(100) | 手机柜位置 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

#### 教师表 (sys_teacher)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| teacher_name | VARCHAR(50) | 教师姓名 |
| teacher_no | VARCHAR(20) | 教师工号 |
| phone | VARCHAR(20) | 联系电话 |
| email | VARCHAR(50) | 邮箱 |
| department | VARCHAR(50) | 所属院系 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

#### 课表 (sys_schedule)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| teaching_class_id | BIGINT | 教学班级ID |
| teacher_id | BIGINT | 教师ID |
| classroom_id | BIGINT | 教室ID |
| course_name | VARCHAR(100) | 课程名称 |
| week_num | INT | 周次 |
| section_num | INT | 节次 |
| schedule_date | DATE | 授课日期 |
| start_time | TIME | 开始时间 |
| end_time | TIME | 结束时间 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

#### 手机没收记录表 (phone_confiscate_record)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| schedule_id | BIGINT | 课表ID |
| teaching_class_id | BIGINT | 教学班级ID |
| teacher_id | BIGINT | 教师ID |
| week_num | INT | 周次 |
| section_num | INT | 节次 |
| confiscate_count | INT | 没收手机数量 |
| confiscate_photo | VARCHAR(255) | 没收照片 |
| confiscate_time | DATETIME | 没收时间 |
| handover_status | TINYINT | 移交状态 |
| handover_confirm_time | DATETIME | 移交确认时间 |
| cabinet_location | VARCHAR(100) | 手机柜位置 |
| remark | VARCHAR(500) | 备注 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### 2.2 E-R图描述
系统主要实体关系：
- 一个学期包含多个班级 (1:N)
- 一个班级可以有多条课表记录 (1:N)
- 一个教师可以有多条课表记录 (1:N)
- 一个教室可以有多条课表记录 (1:N)
- 一条课表记录可以对应多条没收记录 (1:N)
- 一个教师可以有多条没收记录 (1:N)
- 一个班级可以有多条没收记录 (1:N)

## 三、业务流程

### 3.1 教师登记手机流程
1. 教师登录系统
2. 查看近两日授课课堂列表
3. 选择需要登记的课堂
4. 填写没收手机信息（数量、柜子位置、备注等）
5. 提交保存记录

### 3.2 查看记录流程
1. 教师进入记录列表页面
2. 查看所有没收记录
3. 可以修改或删除记录

### 3.3 统计报表流程
1. 教师进入统计报表页面
2. 选择统计维度（按班级、教师、课程等）
3. 查看统计结果和图表展示

## 四、数据库操作（增删改查）

### 4.1 增加记录
```java
// 添加没收记录
@PostMapping("/addRecord")
public String addRecord(PhoneConfiscateRecord record) {
    SysSchedule schedule = scheduleMapper.selectById(record.getScheduleId());
    if (schedule != null) {
        record.setTeachingClassId(schedule.getTeachingClassId());
    }
    record.setTeacherId(loginTeacherId);
    record.setConfiscateTime(LocalDateTime.now());
    record.setHandoverStatus(0);
    record.setCreateTime(LocalDateTime.now());
    recordService.save(record);
    return "redirect:/teacher/recordList";
}
```

### 4.2 查询记录
```java
// 查询没收记录列表
@GetMapping("/recordList")
public String recordList(Model model) {
    LambdaQueryWrapper<PhoneConfiscateRecord> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(PhoneConfiscateRecord::getTeacherId, loginTeacherId)
            .orderByDesc(PhoneConfiscateRecord::getConfiscateTime);
    List<PhoneConfiscateRecord> list = recordService.list(wrapper);
    model.addAttribute("recordList", list);
    return "teacher/recordList";
}
```

### 4.3 修改记录
```java
// 更新没收记录
@PostMapping("/updateRecord")
public String updateRecord(PhoneConfiscateRecord record) {
    record.setUpdateTime(LocalDateTime.now());
    recordService.updateById(record);
    return "redirect:/teacher/recordList";
}
```

### 4.4 删除记录
```java
// 删除没收记录
@PostMapping("/deleteRecord")
public String deleteRecord(@RequestParam Long id) {
    recordService.removeById(id);
    return "redirect:/teacher/recordList";
}
```

## 五、技术实现

### 5.1 开发环境
- 操作系统：Windows 10/11
- 开发工具：IntelliJ IDEA
- JDK版本：Java 17
- 框架版本：Spring Boot 3.2.10
- 数据库：MySQL 8.0
- ORM框架：MyBatis-Plus 3.5.8
- 模板引擎：Thymeleaf

### 5.2 项目结构
```
phone-manage/
├── src/main/java/com/phone/phonemanage/
│   ├── controller/          # 控制器层
│   │   ├── IndexController.java
│   │   ├── TeacherController.java
│   │   └── StatController.java
│   ├── entity/              # 实体类
│   │   ├── PhoneConfiscateRecord.java
│   │   ├── SysSchedule.java
│   │   └── StatReport.java
│   ├── mapper/              # 数据访问层
│   │   ├── PhoneConfiscateRecordMapper.java
│   │   ├── SysScheduleMapper.java
│   │   └── StatReportMapper.java
│   ├── service/             # 业务逻辑层
│   │   ├── ConfiscateRecordService.java
│   │   ├── StatReportService.java
│   │   └── impl/
│   └── PhoneManageApplication.java
├── src/main/resources/
│   ├── application.yml      # 配置文件
│   ├── schema.sql           # 数据库脚本
│   ├── template/            # 页面模板
│   │   ├── teacher/
│   │   │   ├── schedule.html
│   │   │   ├── toAdd.html
│   │   │   ├── toEdit.html
│   │   │   └── recordList.html
│   │   └── stat/
│   │       └── report.html
│   └── static/              # 静态资源
└── pom.xml                  # Maven配置
```

### 5.3 运行说明
1. 确保已安装MySQL数据库
2. 执行 `schema.sql` 脚本创建数据库和表
3. 修改 `application.yml` 中的数据库连接信息
4. 运行 `mvn spring-boot:run` 启动项目
5. 访问 http://localhost:8080 进入系统

## 六、测试结果

### 6.1 功能测试
1. ✅ 教师登录系统
2. ✅ 查看授课课堂列表
3. ✅ 登记没收手机记录
4. ✅ 查看没收记录列表
5. ✅ 修改没收记录
6. ✅ 删除没收记录
7. ✅ 查看统计报表

### 6.2 统计报表测试
1. ✅ 按班级统计
2. ✅ 按教师统计
3. ✅ 按课程统计
4. ✅ 按周次统计
5. ✅ 按节次统计
6. ✅ 按学期统计
7. ✅ 按时间范围统计
8. ✅ 按移交状态统计

## 七、总结

本系统实现了课堂手机管理的基本功能，包括教师角色的手机没收登记、记录管理和统计报表功能。系统采用Spring Boot + MyBatis-Plus + Thymeleaf技术栈，实现了前后端分离的Web应用。

通过本次项目开发，掌握了：
1. Spring Boot项目的搭建和配置
2. MyBatis-Plus的数据访问实现
3. Thymeleaf模板引擎的使用
4. MySQL数据库设计和SQL脚本编写
5. 多维度统计报表的实现

系统满足验收标准要求，功能完整，界面友好，可投入使用。
